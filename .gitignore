node_modules
dist
dist-ssr
*.local
.DS_Store

# native build outputs (regenerated by `npx cap sync`, never hand-edited)
ios/App/App/public
android/app/src/main/assets/public

# signing / secrets -- never commit these
*.p12
*.mobileprovision
*.p8
*.keystore
*.jks
.env
.env.*
release.xcconfig.local

# editor
.vscode/*
!.vscode/extensions.json
.idea

# Capgo OTA signing key — never commit; treat like any other private signing key
.capgo_key_v2
.capgo_key_v2.pub
capgo_key_v2
